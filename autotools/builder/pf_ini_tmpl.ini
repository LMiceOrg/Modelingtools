$def with (ctx)
# Model $ctx.md_cname performance configuration file
# Generated by Modelingtools
# 创建时间：$ctx.tm_now

[model]
#name: $ctx.md_ns $ctx.md_cname
name=$ctx.md_name
cname=$ctx.md_cname
count=$ctx.md_count

#Order:$ctx.it_id
[perf]

$if hasattr(ctx, 'param'):
    $for p in ctx.param:
        #name:$p.it_ns $p.it_cname
        #desc:$p.it_desc
        #unit:$p.it_type: $p.it_unit, ($p.it_min, $p.it_max)
        $p.it_name=$p.it_val

    $# endfor param
$# endif param
